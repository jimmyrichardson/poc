# AI context

Persistent project context for AI agents working in this repo.

| Doc | Purpose |
| --- | --- |
| [vite-and-github-pages.md](./vite-and-github-pages.md) | Local Vite vs GitHub Pages `/poc/` base paths, HTML dist↔dev rewrite, build scripts, adding new POCs |

When changing Vite config, deployment paths, HTML asset tags, or how subdirectories are served, update the relevant doc here and keep `README.md` in sync.
